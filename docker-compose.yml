version: '3.8'

services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./reports:/app/reports
      # Remove static volume mount to avoid conflicts with Docker image
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
